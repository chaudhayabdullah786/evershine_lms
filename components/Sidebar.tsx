/**
 * @deprecated — This component is superseded by the sidebar in app/dashboard/layout.tsx.
 * Retained as a no-op to prevent import errors from any overlooked references.
 */
export default function Sidebar(_props: { user: any; onClose?: () => void }) {
  return null;
}
