/**
 * @deprecated — Use the inline StatCard in app/dashboard/page.tsx which uses
 * ShadCN Card components. This shared version is retained only to prevent
 * import errors from any overlooked references.
 */
export default function StatCard(_props: Record<string, unknown>) {
  return null;
}
